# Document the config file format
`task` `*medium` `!ready`

The settings file looks like this, and the sample below must survive being
written into an entry unchanged:

```markdown
# Not a new entry
## Not a sub-item
- [ ] Not a checklist item
Not a #tag either
```

Everything inside the fence is text, not structure. #docs
