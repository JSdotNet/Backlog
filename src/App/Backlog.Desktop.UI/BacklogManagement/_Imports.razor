@* Backlog Management reads the workspace for where entries live and what is
   turned on. It conforms to the Inbox's published item contract in
   BacklogDrafts, which says so in its own usings rather than here — and it
   never sees Second Brain (Knowledge) or the Shell at all. *@
@using Backlog.Desktop.UI.Workspace
