@* Inbox is upstream of Backlog Management and Second Brain (see
   .domain/context-map.md): it publishes what it triaged and never reads the
   other two back. Deliberately empty — the Inbox needs nothing beyond the
   project-wide imports, and every line added here would be a boundary crossed. *@
