<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application android:allowBackup="true" android:icon="@mipmap/appicon" android:roundIcon="@mipmap/appicon_round" android:supportsRtl="true"></application>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Speech capture on the Inbox screen. The runtime prompt is raised by
         Permissions.RequestAsync<Permissions.Microphone>; this only declares it. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- Android 11 package visibility: without this the recognition service is
         invisible to us and SpeechRecognizer.IsRecognitionAvailable reports
         false on a device that has one installed. -->
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>
</manifest>