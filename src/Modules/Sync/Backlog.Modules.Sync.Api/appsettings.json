{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Azure.Core": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Modules": {
    "Sync": {
      "Tokens": {
        "//SigningKey": "Base64, at least 32 bytes. A secret: it comes from an environment variable, user-secrets, or a key vault, and is never checked in. A Development run with none configured mints a throwaway one and says so in the log.",
        "Issuer": "https://backlog.jsdotnet.dev/sync",
        "Audience": "backlog-sync",
        "LifetimeMinutes": 30
      },
      "Attachments": {
        "MaxBytes": 26214400,
        "//AllowedContentTypes": "Media types an upload may declare, replacing the built-in list (images, PDF, text, Markdown, CSV, Office Open XML) when set. Local ADR 0014."
      }
    }
  }
}
