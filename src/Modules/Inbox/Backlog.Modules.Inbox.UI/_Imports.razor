@* Inbox is upstream of Tasks and Second Brain (see
   .domain/context-map.md): it publishes what it triaged and never reads the
   other two back. Now that the Inbox is its own project these imports are the
   project's own rather than inherited from the desktop app, so the one control
   namespace its pane renders has to be named here. Still deliberately free of
   every context namespace — every line beyond the shared library would be a
   boundary crossed. *@
@using Backlog.UI.Components.Buttons
