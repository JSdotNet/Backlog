/*
    Browser behaviour owned by the shared component library, served from
    _content/Backlog.UI.Components/components.js so a host only has to script it.
    Empty on purpose: a later slice moves the component interop out of the app's
    app.js into here, one function at a time, so the diff stays reviewable.
*/
