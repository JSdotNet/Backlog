// The library's folders each own a namespace, and _Imports.razor names them all
// so a component can move in later without touching every call site. These are
// the ones slice 1 has not filled yet; declaring them keeps the imports valid
// until they do.
namespace Backlog.UI.Components.Badges { }

namespace Backlog.UI.Components.Buttons { }

namespace Backlog.UI.Components.Inputs { }

namespace Backlog.UI.Components.Overlays { }

namespace Backlog.UI.Components.Layout { }
