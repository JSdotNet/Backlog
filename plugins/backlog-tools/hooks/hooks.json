{
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "grep -q -E 'Backlog plan item `|backlog-run-plan-item entry `' && printf '%s\\n' 'This prompt carries a Backlog marker: invoke the backlog-run-plan-item skill before doing anything else with it.' || true"
          }
        ]
      }
    ]
  }
}
