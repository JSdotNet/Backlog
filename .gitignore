## A streamlined .gitignore for modern .NET projects
## including temporary files, build results, and
## files generated by popular .NET tools. If you are
## developing with Visual Studio, the VS .gitignore
## https://github.com/github/gitignore/blob/main/VisualStudio.gitignore
## has more thorough IDE-specific entries.
##
## Get latest from https://github.com/github/gitignore/blob/main/Dotnet.gitignore

# Build results
[Dd]ebug/
[Dd]ebugPublic/
[Rr]elease/
[Rr]eleases/
x64/
x86/
[Ww][Ii][Nn]32/
[Aa][Rr][Mm]/
[Aa][Rr][Mm]64/
bld/
[Bb]in/
[Oo]bj/
[Ll]og/
[Ll]ogs/

# .NET Core
project.lock.json
project.fragment.lock.json
artifacts/

# ASP.NET Scaffolding
ScaffoldingReadMe.txt

# NuGet Packages
*.nupkg
# NuGet Symbol Packages
*.snupkg

# dotenv environment variables file
.env

# Others
~$*
*~
CodeCoverage/

# MSBuild Binary and Structured Log
*.binlog

# MSTest test Results
[Tt]est[Rr]esult*/
[Bb]uild[Ll]og.*

# NUnit
*.VisualState.xml
TestResult.xml
nunit-*.xml

# Scratch space for work in progress: QA evidence, notes, throwaway artifacts.
.wip/

# Cache written by the impeccable design hook.
.impeccable/

# Local QA validation evidence (screenshots, scenario results)
.qa-evidence/
.playwright-evidence/

# Playwright MCP browser output (screenshots, page snapshots, console logs),
# written to the working directory of whichever session drove the browser.
.playwright-mcp/

# Claude Code worktree sessions. The rest of .claude/ (orch-context.md,
# settings.json, hooks/) is tracked team configuration.
.claude/worktrees/

# Local Android developer signing material created by build/Install-AndroidApp.ps1.
# Never commit a keystore or its password; release signing comes from repo secrets.
build/.local/

# The vendored Archify CLI. `[Bb]in/` above is a .NET build-output rule and it
# swallowed `tools/archify/bin/` when Archify was vendored, so the four
# entrypoints it holds were never committed — including the `archify.mjs` that
# `tools/diagrams/archify-artifacts.mjs` spawns to render every diagram artifact.
# Regeneration failed with MODULE_NOT_FOUND on any fresh clone while `scan` and
# `verify`, which never shell out, both reported green. The negation names the
# directory rather than its contents because git does not look inside an excluded
# directory, so `!tools/archify/bin/**` on its own would keep it invisible.
!tools/archify/bin/
