<Project Sdk="Microsoft.NET.Sdk">

  <!-- No project references on purpose: these tests read the repository layout
       itself, so adding a reference would defeat what they are checking. -->

</Project>