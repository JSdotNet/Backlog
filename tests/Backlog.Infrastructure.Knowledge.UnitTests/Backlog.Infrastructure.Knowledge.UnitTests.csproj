<Project Sdk="Microsoft.NET.Sdk">

  <!--
    The reader's own suite. It holds the cross-language contract ADR 0004 names
    as this change's main risk: every database these tests read is created from
    the DDL in tools/knowledge/knowledge-schema.mjs, read from disk at test time,
    so a column renamed on the writing side fails here rather than in a panel.
  -->
  <ItemGroup>
    <ProjectReference Include="..\..\src\Infrastructure\Backlog.Infrastructure.Knowledge\Backlog.Infrastructure.Knowledge.csproj" />
  </ItemGroup>

  <ItemGroup>
    <Using Include="Backlog.Infrastructure.Knowledge" />
  </ItemGroup>

</Project>
